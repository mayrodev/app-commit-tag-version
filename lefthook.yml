pre-push:
  commands:
    require-changelog:
      run: |
        if ! git diff --name-only @{u}..HEAD | grep -q "CHANGELOG.md"; then
          npx commit-and-tag-version --dry-run --noBumpWhenEmptyChanges 2>&1 | grep -q "no commits found" || exit 1
        fi
